[project]
name = "my-webrtc-builds"
version = "0.1.0"
description = "Reproducible WebRTC M150 build orchestration"
requires-python = ">=3.11"
license = "Apache-2.0"

[tool.ruff]
line-length = 100
target-version = "py311"

